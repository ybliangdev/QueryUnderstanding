from typing import List, Tuple


def merge_bio_tags(tokens: List[str], tags: List[str]) -> List[Tuple[str, str, int, int]]:
	"""Placeholder for Phase 3: merge BIO tags to entity spans.

	Returns a list of tuples: (entity_text, entity_type, start_token_idx, end_token_idx).
	"""
	spans: List[Tuple[str, str, int, int]] = []
	# Implementation will be added in Phase 3
	return spans


